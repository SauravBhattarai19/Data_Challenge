# ── Data (raw inputs, too large for GitHub) ──────────────────────────────────
Data/

# ── Generated / processed data outputs ───────────────────────────────────────
# App-required files in data_processed/ ARE tracked (all under GitHub's 100 MB limit).
# Exclude files that are not loaded by the Streamlit app or exceed the 100 MB limit:

# 131 MB shapefile — over GitHub's hard limit
project/data_processed/raw/tl_2022_us_county/

# Raw parquet files not loaded by the Streamlit app
project/data_processed/raw/igs_all_years.parquet
project/data_processed/raw/igs_latest.parquet
project/data_processed/raw/fema_nri.parquet
project/data_processed/raw/svi.parquet
project/data_processed/raw/cdc_places.parquet
project/data_processed/raw/food_atlas.parquet
project/data_processed/raw/eji.parquet
project/data_processed/raw/ahrf_county.parquet
project/data_processed/raw/cbp_naics_county.parquet
project/data_processed/raw/fqhc_county_counts.parquet
project/data_processed/raw/hpsa_pc_county.parquet
project/data_processed/raw/hpsa_mh_county.parquet
project/data_processed/raw/mua_county.parquet

# Build-stage files not loaded by the Streamlit app
project/data_processed/master_tract.parquet
project/data_processed/shap_values.parquet
project/data_processed/igs_improvement_model.parquet

project/reports/community_profiles/

# ── Python artifacts ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
dist/
build/

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
.streamlit/secrets.toml

# ── Claude Code artifacts ─────────────────────────────────────────────────────
.claude/

# ── IDE artifacts ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS artifacts ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Planning & personal notes (root-level markdown) ───────────────────────────
claude_opus_plan.md
claude_plan.md
knowledge.md
last_brainstorm.md
plan.md
presentation_idea.txt
presentation_script_final.txt
